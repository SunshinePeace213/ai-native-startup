- [S:critical] [C:99] spec.md / Project targeting — A directory counts as a project only when `sign-off/` already exists, so a sole project missing that directory is treated as zero projects and fails open instead of blocking for missing sign-off — fix: identify projects independently of `sign-off/` and test that a missing sign-off directory blocks.
- [S:minor] [C:100] spec.md / Sign-off document — The mandatory SHA-256 example uses abbreviated values containing `…`, contradicting the required 64-hex format — fix: use complete 64-character hexadecimal hashes.
- [S:major] [C:98] spec.md / Change-order contract; acceptance-criteria.md / AC13 — The change-order schema requires request, round/time cost, and signature fields, but AC13 accepts any existing file without validating them — fix: parse those fields and add failing tests for incomplete or unsigned change orders.
- [S:major] [C:99] tasks.md and acceptance-criteria.md / AC16 — The proposed Markdown eval files under `specs/cpo-layer/evals/` have no executable runner and do not use the meta-skills harness’s committed `evals/evals.json` format, so the claimed repeated-run pass rates are not reproducible — fix: define harness-compatible eval suites and exact commands that execute and grade them.
- [S:major] [C:98] checks/ac3-role-agents.sh / tool restriction — The line-based check misses a multiline `tools:` list containing `Agent`, allowing a role with nesting capability to pass — fix: parse frontmatter as YAML and assert `disallowedTools` contains `Agent` for every role.
- [S:major] [C:99] tasks.md / studio-phase-commands; spec.md / Requirement 3 — P2 still instructs the builder to “spawn one teammate,” contradicting the fixed policy of ordinary subagents and “no agent teams anywhere” — fix: replace the remaining teammate instruction and stale teammate rationale with the cold-designer subagent flow.
- [S:major] [C:98] acceptance-criteria.md / AC11–AC12 — Requiring nonempty declared tables still permits a one-component matrix or one token pair to pass while omitting the rest; no authoritative component or usage inventory is defined or cross-checked — fix: define those inventories and test that every required component, pair, and target is present.
- [S:minor] [C:96] checks/ac4-client-artifacts.sh / page-pattern labels — Substring matching allows one row to contain multiple required labels while an unrelated fourth row passes, so the four required rows are not enforced one-to-one — fix: normalize labels and compare their exact multiset with the four expected labels.
- [S:minor] [C:100] tasks.md / Validate Everything — It requires the unscoped rules plus `AGENTS.md` to remain under ~250 lines, although the current baseline is 301 lines, while AC5 measures only rules and permits 280 — fix: apply the documented budget only to unscoped rules and state a separate concise-pointer check for `AGENTS.md`.
- [S:minor] [C:99] spec.md and decisions.md / interactive rounds — The unsupported claim that “a teammate cannot prompt the user” remains, despite the ledger saying it was removed — fix: remove the remaining claim and base the principal-only rule on the documented subagent limitation.
- [S:minor] [C:99] decisions.md / KB gap — The plan explicitly admits that custom-command frontmatter lacks a cached official source and defers it, violating spec-standard 6 for the `kb-grounded` profile — fix: mirror and cite the official custom-command documentation before approval.
- [S:minor] [C:100] acceptance-criteria.md / Validation Commands — AC2, AC8, and AC10 still run whole pytest files; listing suffixes after the AC8 command does not make them executable node IDs — fix: replace every criterion-specific pytest invocation with complete `path::test_name` arguments, retaining whole-suite execution only for AC14.
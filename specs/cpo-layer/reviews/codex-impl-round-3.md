[S:major] [C:99] .claude/commands/studio-layer/evals/evals.json::b4 — I2-F2 remains open: `sections()` counts the document title and arbitrary metadata bullets as sections, while traceability searches the entire body; `# Cold Designer Plan`, `- Audience`, `- Constraints` plus matching triage rows passes without any section-level plan — fix: parse an explicit section inventory, exclude titles/metadata, require triage rows to match it, and add this counterexample as a regression test.

[S:major] [C:99] specs/cpo-layer/spec.md::Revision log/change order — I2-F4’s revised contract requires project-relative change-order files, but current `check_revision_count.py` joins unchecked references at line 263, accepting absolute paths, `..`, and escaping symlinks; one project can therefore borrow another project’s order — fix: reject absolute and `..` references, require the resolved file beneath the project root, and add absolute/traversal/symlink tests.

[S:major] [C:100] specs/cpo-layer/summary.md::Status/AC16 — The new summary and `artifacts/dev-report.html` say AC16 has no valid rate and list E-F1/S-F1/S-F2 as open, while current `implementation-notes.md` records a valid 1.0/1.0 run and those fixes; `spec.md` was changed to “ready for review” but retains the obsolete 0.8889 gap — fix: regenerate the summary and report from the final ledger/notes, remove the stale spec gap, and update the suite evidence from 1030 to 1032 tests.
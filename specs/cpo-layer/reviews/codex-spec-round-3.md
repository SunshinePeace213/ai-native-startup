[S:major] [C:99] acceptance-criteria.md / AC16 — The commands suite has no runner: `ac16-evals-are-runnable.sh` only validates JSON, while running assertion checks directly neither executes each prompt nor produces outputs to grade — fix: add an executable repeated-run command-eval harness that invokes each command prompt in isolation, collects outputs, runs assertions, and records pass rates.

[S:major] [C:98] spec.md / Component inventory; acceptance-criteria.md / AC11–AC12 — The inventory is merely declared authoritative and can itself omit nine of ten components, so a one-component inventory, matrix, and token table still pass; no task derives it from or includes it in the signed P3 artifacts — fix: make the inventory a required P3 artifact derived from the structure deliverables, require its hash in P3 sign-off, and validate P6 against that signed inventory.

[S:major] [C:97] acceptance-criteria.md / AC13 — AC13 still says any present change-order document allows the revision, contradicting the new required-field contract and tests for incomplete or unsigned documents — fix: require a complete, parseable change order for the allow path and state that missing or invalid required fields fail.
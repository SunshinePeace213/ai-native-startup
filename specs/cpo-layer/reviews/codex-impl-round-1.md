- [S:major] [C:100] specs/cpo-layer/implementation-notes.md / AC16 manual eval — The command suite exited 1 and scored 0.8889 against its required 1.0, so AC16 and implementation standard 2 are unmet — fix: reconcile the disputed Budget assertion with the documented contract, rerun three times, and record a passing rate.
- [S:major] [C:100] specs/cpo-layer/artifacts/build-brief.html / Outcome and AC16 ledger — The brief claims “16 of 16 with evidence” and defaults to accepting all criteria while its AC16 row is pending and the later recorded eval failed — fix: regenerate the brief after resolving AC16, showing the actual rate and blocking acceptance while it fails.
- [S:major] [C:99] .claude/hooks/check_gate_signoff.py / check_signoff and phase dispatch — The hard gates accept any one signed artifact; tests deliberately sign `docs/<phase>-approved.md`, so P2 can close without its briefs/sitemap, P3 without wireframes, P4 without the picked direction, and P6 without the handoff pack — fix: define and enforce each phase’s required artifact paths and test every missing required artifact.
- [S:major] [C:100] .claude/scripts/studio-layer/check_revision_count.py:74-81 and tests/harness-layer/studio-layer/test_studio_checks.py:425-433 — The allowance is read from the mutable current brief without verifying its P2-signed SHA; directly changing 2 to 3 is tested as an allow path, bypassing change orders — fix: require the project brief in `sign-off/p2.md`, verify its hash before reading the allowance, and make post-signature mutation fail.
- [S:major] [C:99] .claude/scripts/studio-layer/check_revision_count.py:84-99,152-163 — Round numbers are trusted rather than counted or validated, so repeated rows numbered `1` never exceed the allowance — fix: require positive, unique, contiguous round numbers and add duplicate, skipped, zero, and negative-number tests.
- [S:major] [C:99] .claude/scripts/studio-layer/check_revision_count.py:114-133,152-163 — `Cost — rounds: 0` is accepted, and one one-round change order can be reused by unlimited excess rounds — fix: require a positive cost and ensure each change order’s purchased-round capacity covers the excess rows referencing it.
- [S:major] [C:99] .claude/commands/studio-layer/*.md / Variables and hard-gate instructions — Commands use repo-relative `clients/$1` and `.claude/...` paths but instruct multi-project users to run inside `PROJECT_DIR`, where those paths resolve incorrectly — fix: anchor every project, rule, and script path to the resolved repository root.
- [S:major] [C:98] .claude/commands/studio-layer/*.md / PROJECT input — Only a missing project is rejected; `../` segments, absolute paths, hidden segments, or extra path levels can redirect writes outside `clients/<client>/<project>` — fix: require exactly two safe non-dot segments and verify the resolved directory remains under `clients/`.
- [S:major] [C:99] .claude/hooks/check_gate_signoff.py:112-114,188-201 — Artifact paths are not constrained to the project, so absolute paths, `../` traversal, and escaping symlinks can satisfy a client sign-off using files elsewhere — fix: reject absolute or parent-relative paths and require the resolved target to remain beneath the resolved project root.
- [S:major] [C:99] .claude/hooks/check_gate_signoff.py:282-299 — QA rows with blank or malformed values such as `blocking | TBD` or `blocker | open` silently allow P6 because only the exact pair `blocking/open` blocks — fix: validate every severity and status against the documented enums and block malformed rows.
- [S:major] [C:99] .claude/scripts/studio-layer/check_contrast.py:150-215 / handoff token schema — Token coverage searches the entire row, including `Used for`, while the documented example permits bare hex cells; an unchecked token can therefore pass merely by being mentioned outside Foreground/Background — fix: define an explicit token-to-colour mapping in those cells or dedicated columns, parse it structurally, and test a token mentioned only in `Used for`.
- [S:major] [C:99] .claude/scripts/studio-layer/run_command_evals.py:153-174 — The staged project omits `.claude/rules/harness-layer/artifacts.md`, even though the staged `client-artifacts.md` explicitly inherits it, so P1/P2 are evaluated with a broken dependency — fix: stage that referenced rule and assert the relative link resolves in the scratch project.
- [S:major] [C:98] .claude/scripts/studio-layer/run_command_evals.py:329-379 — A `claude -p` envelope marked as an error is merely recorded as `run_error`; its assertion score still contributes normally and can make the runner exit 0 — fix: score errored executions as zero or fail the case independently of produced partial files.
- [S:minor] [C:97] .claude/scripts/studio-layer/check_revision_count.py:24,74-81 — The allowance regex accepts any line beginning with an integer and does not require the locked `(plus polish)` schema — fix: full-match the complete documented line and return exit 2 for malformed baselines.
- [S:minor] [C:98] .claude/scripts/studio-layer/check_states_matrix.py:73-115 — Short inventory or matrix rows can raise uncaught `IndexError`, producing a traceback/exit 1 instead of the contracted diagnostic exit 2 — fix: use bounds-safe cell access and convert malformed rows into `ParseError`.
- [S:minor] [C:96] .claude/scripts/studio-layer/check_states_matrix.py:98-116 — Duplicate component/breakpoint rows overwrite earlier rows, allowing a later filled row to hide an earlier contradictory or blank specification — fix: reject duplicate keys and add a duplicate-row regression test.
- [S:minor] [C:96] .claude/scripts/studio-layer/run_command_evals.py:323-345 — Reusing `--workspace` leaves old output files in each run directory, so judge-only assertions can grade stale artifacts from a prior invocation — fix: require an empty workspace or recreate each run’s output directory before collection.
- [S:minor] [C:95] .claude/scripts/studio-layer/run_command_evals.py:95-150,467-480 — Validation does not reject duplicate case IDs or non-scalar IDs; duplicates collide in directories/rate maps and unhashable assertion IDs crash instead of returning exit 2 — fix: validate scalar, unique case and assertion IDs and add malformed-schema tests.
- [S:minor] [C:100] .claude/rules/memory-series.md and specs/cpo-layer/tasks.md / validate-all — The implementation adds three lines to the unscoped rules, growing the stated 254-line baseline to 257 despite the task’s explicit “must not grow” requirement — fix: revert this unrelated lesson edit or offset only with in-scope reductions.
- [S:minor] [C:99] specs/cpo-layer/checks/ac5-rules-path-scoped.sh:30-37,67-71 — AC5 uses a substring grep that accepts invalid scopes such as `clients/**bogus` and relaxes the ~250/no-growth budget to 280, so the check can pass the violation above — fix: parse YAML, validate the exact supported scope, and enforce the specified baseline/budget.
- [S:info] [C:94] .claude/hooks/check_gate_signoff.py / P6 dispatch — P6’s Stop gate never executes the existing states and contrast checks, so a signed handoff with failing matrices or tokens can still close if the command skipped its prose instructions — fix: make the P6 gate invoke both read-only checks, creating one enforcement point for the countable claims.
- [S:info] [C:92] .claude/hooks/check_gate_signoff.py:6-26 and .claude/scripts/studio-layer/run_command_evals.py:6-33 — Long rationale-heavy module headers duplicate the plan and catalog instead of staying as KISS operational instructions — fix: retain only usage, inputs, outputs, and exit semantics, removing design-history prose.
- [S:critical] [C:99] spec.md / Interfaces & Contracts — `check_gate_signoff.py p2` receives a phase but no client/project identifier, while Stop input provides no command arguments, so it cannot deterministically choose among multiple client projects — fix: define project targeting, such as requiring `cwd` to be `clients/<client>/<project>`, derive it from Stop JSON, reject other locations, and test two simultaneous projects.
- [S:major] [C:99] spec.md / Sign-off document — one `Artifact SHA` cannot verify the example’s two approved files, and `Approves` is not required or path-resolved despite AC8 requiring the hook to hash what it names — fix: replace both fields with a mandatory artifact table containing one project-relative path and SHA-256 per artifact.
- [S:major] [C:98] spec.md / Interfaces & Contracts — the states matrix, token/pair table, tap-target data, discovery notes, triage rows, signed revision allowance, revision log, and change-order linkage lack exact schemas and paths, leaving implementers and tests free to invent incompatible formats — fix: specify each machine-readable Markdown schema and have phase commands and contract tests use those exact formats.
- [S:major] [C:99] tasks.md / studio-phase-commands — P1 never invokes `check_question_coverage.py`, leaving the objective’s question-coverage mechanism orphaned — fix: require P1 to run it before its soft gate and add an eval or structural assertion proving the invocation.
- [S:major] [C:97] tasks.md / studio-role-agents and studio-phase-commands — `studio-design-qa` is said to block handoff, but P6 merely spawns it; neither the Stop hook nor any acceptance criterion requires a passing QA report — fix: define the QA report contract, make unresolved blocking findings prevent P6 completion, and add an agent/command eval.
- [S:major] [C:99] tasks.md / studio-question-bank and studio-phase-commands — the skill and eight commands are nondeterministic prose but have no eval-harness files or repeated-run rubric, contrary to `test-tiers.md` — fix: add manual eval cases and rubrics to both tasks’ Files and validation commands.
- [S:major] [C:96] spec.md / Task Description; tasks.md / studio-role-agents — the promised one-level-deep architecture is unenforced because custom subagents inherit the Agent tool by default and the role task never denies it — fix: remove Agent from each role’s tools or add it to `disallowedTools`, then assert that restriction.
- [S:major] [C:98] decisions.md / Assumptions — “either spawn shape works” is false for effort stamps: teammate reuse honors the definition’s tools and model, while teammates inherit the lead’s effort rather than the agent file’s effort — fix: keep stamped roles as subagents, or explicitly redesign and test teammate effort handling.
- [S:info] [C:99] decisions.md / cold-designer teammate — a normal non-fork subagent already starts without the lead’s conversation history, so the experimental agent-team dependency adds no isolation benefit here — fix: run the cold-designer check as an ordinary foreground subagent.
- [S:major] [C:98] spec.md / Requirements 3; decisions.md / Assumptions; tasks.md / studio-phase-commands — the plan alternately says P2 is the only teammate, calls P4 the clear teammate case, and specifies teammate behavior only for P2 — fix: lock one spawn policy and reflect it consistently in requirements, tasks, and acceptance criteria.
- [S:major] [C:99] tasks.md / studio-role-agents — the task refers to “the phase table in spec.md,” but no such table exists; phase documents, staffing, and role ownership are only embedded in discovery HTML — fix: add the authoritative phase/document/role table to spec.md and reference it directly.
- [S:major] [C:98] tasks.md / gate-signoff-hook — missing or unrecognized phase arguments are configured to exit 2 even though the repository hook contract classifies registration/usage mistakes as plumbing failures that must fail open — fix: return 0 for invalid hook configuration and let wiring tests prevent bad registrations.
- [S:major] [C:97] tasks.md and acceptance-criteria.md / AC8 — no behavior or test is defined for `stop_hook_active`, although Stop hooks must handle re-entry and Claude Code forcibly ends after eight consecutive blocks — fix: define and test re-entry behavior and state the gate’s bounded runtime semantics.
- [S:major] [C:99] specs/cpo-layer/checks/ac1-client-data-home.sh — both “before” and “after” runs already contain client folders and the script only compares exit codes, so a reverted hook that wrongly blocks whenever any client exists can return 2 twice and pass — fix: measure a no-client baseline, add the first client only afterward, and assert both results are exactly 0.
- [S:minor] [C:100] specs/cpo-layer/checks/ac1-client-data-home.sh — the validator uses `rm -rf` twice, violating the repository’s absolute safe-delete rule — fix: use exact non-recursive cleanup where possible or move generated directories to `~/.Trash/` via traps.
- [S:major] [C:99] specs/cpo-layer/checks/ac4-client-artifacts.sh — AC4 can pass with extra table rows, only one early-phase token, labels outside the table, and a single generic copy-as-prompt mention — fix: parse the intended table structurally and assert exactly four rows, both phase bands, and a return contract in every row.
- [S:minor] [C:97] specs/cpo-layer/checks/ac4-client-artifacts.sh — the “original untouched” assertion silently skips when neither `origin/main` nor `main` resolves, allowing the validator to pass without checking the guardrail — fix: make absence of a comparison base fail or replace the branch comparison with a checked-in content/drift assertion.
- [S:major] [C:99] specs/cpo-layer/checks/ac5-rules-path-scoped.sh — it scans only top-level `*.md`, while Claude discovers rules recursively, so an unscoped nested studio rule passes AC5 — fix: recursively enumerate every Markdown file below `.claude/rules/studio-layer/` and parse its YAML frontmatter.
- [S:major] [C:99] specs/cpo-layer/checks/ac7-phase-commands.sh — the check only requires a top-level `hooks:` key, not that the registration is under `hooks.Stop`; a PreToolUse registration would pass — fix: parse YAML and assert the exact `hooks.Stop[*].hooks[*]` event, type, command, and phase argument.
- [S:major] [C:97] acceptance-criteria.md / AC11–AC12 — the state and contrast checks quantify only declared components, pairs, breakpoints, and targets, allowing empty or incomplete inventories to pass vacuously — fix: define authoritative component and usage inventories, require nonempty tables, and cross-check every inventory item.
- [S:major] [C:96] tasks.md / studio-namespace; acceptance-criteria.md / AC5 — the locked studio-identity content and AGENTS.md pointer are untested; empty identity prose with valid `paths:` passes all checks — fix: add a drift criterion for name, voice, letterhead, sign-off contract, and hub pointer, plus an eval proving client documents apply them.
- [S:major] [C:82] tasks.md / studio-question-bank — the plan assumes `name: studio-client-questions` is the invocation identity despite placing the skill under `studio-layer/client-questions`; the cited documentation says the directory name becomes the command, and the validator never attempts invocation — fix: align the directory with the intended command name or add grounded discovery/invocation tests for the nested path.
- [S:major] [C:95] decisions.md / Assumptions; acceptance-criteria.md / AC12 — the plan claims WCAG behavior while explicitly deferring the WCAG mirror and hard-codes “commonly published” thresholds without definitions or exceptions — fix: mirror and cite the official WCAG source now, or describe these as project-specific thresholds rather than WCAG compliance.
- [S:major] [C:97] spec.md / Task Description; decisions.md / Open Questions — the plan says every decision is locked and nothing is re-interviewed, yet the P5 prototype tool remains an open decision deferred to the first run — fix: resolve it now or define it explicitly as a runtime input with selection rules and acceptance coverage.
- [S:minor] [C:99] spec.md / Check script CLI and Edge Cases — exit 2 is said to be reserved for missing/unreadable arguments, but malformed hex and missing revision allowance also use it — fix: define exit 2 consistently as usage, parse, or invalid-input failure.
- [S:minor] [C:99] spec.md / Edge Cases — “every studio check” allegedly exits 0 when `clients/` is absent, contradicting the explicit CLI contract that a missing target is exit 2 — fix: restrict the no-client fail-open rule to the command-scoped hook.
- [S:minor] [C:99] tasks.md / Validate Everything — the task declares `Files: none — read-only` but then writes results to `implementation-notes.md` — fix: list that file and mark the task as evidence-writing.
- [S:minor] [C:100] spec.md / Tracking — `PR: filled by /harness-layer:harness-build` is a placeholder in a section whose standard explicitly permits no placeholders — fix: remove the PR row until a concrete number exists or record the actual PR.
- [S:minor] [C:99] tasks.md / Validate Everything — it requires AGENTS.md’s always-loaded budget to be “unchanged” after another task explicitly adds a Studio Layer section — fix: require the budget to remain below its limit, not unchanged.
- [S:minor] [C:98] spec.md / Risk & Rollback — “two changes reach outside the new namespace” omits `.gitignore`, AGENTS.md, shared tests, and the hook catalog — fix: enumerate every cross-namespace/global modification and its rollback impact.
- [S:minor] [C:91] decisions.md / KB References — the cited agent-team lines establish independent context and lack of delegated consent, not the claim that a teammate cannot prompt the user; the same document describes direct user interaction with teammate sessions — fix: remove that claim or cite explicit cached documentation for the actual limitation.
- [S:minor] [C:84] decisions.md / KB References — no listed source grounds all relied-on command-frontmatter and argument semantics, notably `argument-hint` and command argument expansion — fix: add a cached official command/skill-frontmatter reference supporting each field or remove unsupported fields.
- [S:info] [C:98] decisions.md / KB References — the HTML-artifacts article supports interactive two-way pages and copy-as-prompt, but not the project-specific four page-pattern rows the table says it grounds — fix: narrow the Grounds cell and identify the four mappings as project decisions.
- [S:minor] [C:95] acceptance-criteria.md / Validation Commands — criterion-specific pytest commands use whole files or `-k` filters rather than exact node IDs, contrary to `impl-standards.md`’s evidence format — fix: list exact `tests/...::test_name` node IDs for each criterion while retaining the full-suite command only for AC14.
- [S:minor] [C:78] spec.md / studio rule inheritance — `paths: clients/**` rules load lazily on matching file access, so path scope alone does not prove that rules are present when creating the first file in a new client project — fix: have each relevant phase command explicitly read the identity/artifact rules and cover first-project creation in an eval.
### Round 6 — Verdict: changes-requested

- **The intake “blocked until complete” guarantee still exceeds the documented Stop-hook contract.** The objective, decisions.md DoR targeting decision, spec.md DoR edge case, task 4, and AC8 require the hook to keep an incomplete intake from ending indefinitely. `ai-docs/anthropic/hooks.md` says Claude Code overrides a Stop hook after eight consecutive blocks, and `ai-docs/anthropic/hooks-guide.md` explicitly documents that the turn then ends unless the cap is raised. Retaining the marker fixes round 5's repeated-firing bug, but it cannot make the stated absolute gate true: after eight no-progress continuations, Claude Code can end while `intake.md` is still incomplete even if the script keeps returning exit 2. Fix: revise those sections to an explicitly platform-bounded guarantee and specify/test the block-cap behavior (including any required `CLAUDE_CODE_STOP_HOOK_BLOCK_CAP` configuration), or replace the Stop-only design if an absolute “cannot end incomplete” guarantee is non-negotiable.
- **The validation suite still permits builds that violate AC1, AC3, and AC10.** AC1's `git log ... | head` check proves only that some commit touched the spec folder; it does not prove PR #49 referenced #43, merged, or landed before any child pipeline started. AC3 validates the five `design` entries thoroughly but checks only the memory entry's URL, so a missing `file`, null `fetched`, or absent `ai-docs/index.md` entry still passes despite the criterion applying those requirements to it. AC10 accepts `public link` without asserting client consent and accepts `best-effort` without asserting publishing never blocks, so the safety-critical delivery contract can be weakened and still pass. Fix: in acceptance-criteria.md, add observable PR/timeline checks for AC1, apply the full file/fetched/index assertions to the anthropic memory entry in AC3, and require consent plus non-blocking semantics in AC10's parsed `Publish:` field.

**Issue-comment digest:** Round 6, changes-requested — 2 blocking: the absolute intake gate conflicts with Claude Code's documented eight-block Stop-hook cap, and AC1/AC3/AC10 still have validation gaps that permit contract-violating builds. Next: reconcile the DoR guarantee with the platform cap, strengthen those three checks, then re-review.

### Round 5 — Verdict: changes-requested

- **The successful intake check can delete its marker before the turn is actually allowed to stop.** `ai-docs/anthropic/hooks.md` states that all matching hooks run in parallel and that an exit-2 `Stop` result prevents stopping and continues the conversation; this repo also has the independent blocking `Stop` hook in `.claude/settings.json`. Under the specified task 4/AC8 behavior, the intake hook can find a complete intake, delete its own marker, and return 0 while that other hook returns 2. On the next `Stop`, the intake hook finds no own-session marker and must return 2, stranding the command. Fix: revise decisions.md's DoR targeting decision, spec.md's DoR edge case, task 4, and AC8 so successful readiness remains idempotently provable across repeated `Stop` firings (for example, retain the harmless session-scoped marker rather than deleting it), and add a regression where another `Stop` hook blocks once between two successful readiness checks.

**Issue-comment digest:** Round 5, changes-requested — 1 blocking: readiness cleanup can delete the session marker before another parallel Stop hook forces a continuation, leaving the next readiness check permanently blocked. Next: make successful readiness idempotent across repeated Stop firings and add the cross-hook continuation regression, then re-review.

### Round 2 — Verdict: changes-requested

- **The round-1 intake-targeting blocker remains under concurrent intake runs.** decisions.md's round-1 fix uses one shared `projects/.intake-target`, overwritten by every invocation, so concurrent `/soriza-design:intake A` and `/soriza-design:intake B` sessions can still make either command's Stop hook gate the other client. The cross-client test in task 4/AC8 covers sequential A/B state, not this overwrite race. Fix: key the target state by the hook input's session identity (or pass the invoked slug through another invocation-scoped channel), update decisions.md/spec.md/task 4/AC7–AC8, and add a two-session regression proving each hook gates its own client.
- **The ladder's final rung is specified and validated against the wrong predecessor.** spec.md and decisions.md lock a chain where every rung reads the previous rung, but acceptance-criteria.md's AC9 assertion maps `section-briefs` to `sitemap-ia.md` rather than `wireframes/`; that lets the build skip the wireframe rung's output and still pass. Fix: make task 5 and AC9 require `section-briefs.md` to consume `wireframes/` (while retaining `sitemap-ia.md` as additional inventory context if needed), and update the validation mapping.
- **The round-1 validation blocker is only partially resolved.** The new assertions materially strengthen AC4, AC6, and AC8, but AC7 and AC9–AC11 still pass on loose keyword presence: they do not prove `.intake-target` is the first write, the exact predecessor/refusal/commit contracts, wireframe best-effort publishing plus all three locked delivery modes and no-external-dependency requirement, the section-brief packet contents, or the git lane's two exact gate points and evidence replacement. Fix: replace those keyword checks in acceptance-criteria.md with clause-level assertions or focused contract tests that fail when each required instruction is absent or contradicted, and keep task 7 running them.

**Issue-comment digest:** Round 2, changes-requested — 3 blocking: shared intake target state is still race-prone, the final rung validates the wrong predecessor, and AC7/AC9–AC11 remain under-tested. Next: make targeting invocation-scoped, correct the rung dependency, strengthen those contract checks, then re-review.

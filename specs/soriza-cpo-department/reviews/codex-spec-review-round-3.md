### Round 3 — Verdict: changes-requested

- **The revised intake target is still not invocation-scoped and can strand unrelated runs.** decisions.md's “DoR gate targeting is deterministic” decision, spec.md's DoR edge case, task 4, and AC8 make every Stop hook gate every marked client. If concurrent client B is interrupted and leaves an incomplete marker, completed client A can never finish; the only specified sweep removes markers for already-complete clients, so the abandoned incomplete marker has no recovery path. Fix: key target state to the hook input's session identity (with collision-safe state for concurrent runs of the same client), make each Stop invocation resolve and clean up only its own target, and revise decisions.md, spec.md, task 4, AC7–AC8, and the concurrent regression to prove A and B can complete independently without either releasing the other.
- **The round-2 validation blocker remains partially unresolved in AC9–AC11.** AC9 accepts any non-empty `DoR gate:`, accepts `Refusal: refuse` without requiring the missing artifact to be named, and accepts a `Commit:` containing either “engagement branch” or `docs(` without proving the full per-rung commitment; AC10's inline/fan-out/copy clauses remain loose whole-file keyword checks; AC11 finds gate names and `Refs #`/`Closes #` independently, so swapped closing semantics can pass. A command or lane rule that violates the locked contracts can therefore pass task 7. Fix: make acceptance-criteria.md parse structured contract fields and assert the exact per-rung DoR/refusal/commit clauses, encode AC10's inventory/copy conditions in those fields or focused tests, and assert the two AC11 gate-point mappings directly (`brief approved` → `Refs #N`, `packet hand-off` → `Closes #N`) with failures for missing or contradictory clauses.

**Issue-comment digest:** Round 3, changes-requested — 2 blocking: all-client marker gating can strand unrelated intake runs, and AC9–AC11 still do not enforce several locked command and git-lane clauses. Next: make intake targeting session-scoped and strengthen those assertions, then re-review.

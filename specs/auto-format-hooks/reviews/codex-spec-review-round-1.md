### Round 1 — Verdict: changes-requested

- **WorktreeRemove payload handling contradicts the cached official docs.** `tasks.md` Step 4 and `acceptance-criteria.md` AC8 require `worktree_remove.py` to read `{"worktree_path": "<p>"}`, but `ai-docs/anthropic/hooks.md` documents the WorktreeRemove event payload with `worktreePath`, and `ai-docs/anthropic/worktrees.md` points WorktreeRemove readers back to that hooks reference for the input schema. As written, the implementation can pass the synthetic AC8 test while missing the real documented payload. Fix: update `spec.md`, `tasks.md`, and AC8 to either use documented `worktreePath` or explicitly support both `worktreePath` and the reference-implementation `worktree_path`, and add validation using the documented field.
- **Several validation commands can pass without verifying their claimed acceptance criteria.** In `acceptance-criteria.md` `## Validation Commands`, the AC1 ESLint fixture command ends with `; rm -f ...`, so a failed `eslint --fix` can be hidden by a successful cleanup; the AC9 `grep -c 'auto-format' .claude/settings.json` prints a count but does not assert `>= 6`; and the AC11 command runs only `tests/harness-layer/hooks/` even though AC11 says the full pytest run passes. Fix: make the AC1 command fail on ESLint failure, assert the auto-format count, and either change AC11 to the hook pytest suite or run the actual full pytest command.

**Recommendations (advisory, non-blocking):**

- Add the cached docs this plan also depends on to `decisions.md` `## KB References`: `ai-docs/anthropic/skills.md` for the `meta-install` skill, `ai-docs/anthropic/agent-teams.md` for the TaskCreate/TaskUpdate team plan, and `ai-docs/anthropic/settings.md` for project settings behavior.
- Consider simplifying the execution plan if agent-team orchestration is not guaranteed for the build session. Two builders plus a validator is reasonable only if the team tools are definitely enabled; otherwise the same ordered task list can be executed by one builder without changing scope.

**Issue-comment digest:** Round 1, changes-requested — 2 blocking: WorktreeRemove uses an undocumented `worktree_path` payload while the cached docs specify `worktreePath`, and validation commands can pass without proving AC1, AC9, and AC11. Next: revise the payload contract/tests and tighten the validation commands, then re-review.

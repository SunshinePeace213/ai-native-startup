### Round 2 — Verdict: changes-requested

- **The taste route depends on an unavailable AskUserQuestion preview contract.** tasks.md Step 2 and AC1 require `preview` options during the pre-worktree grilling flow, but `ai-docs/anthropic/agent-sdk/user-input.md` documents option previews only for TypeScript Agent SDK hosts that set `toolConfig.askUserQuestion.previewFormat`; without that setting the field is absent. This target is a Claude Code command, and no task introduces an SDK host or that configuration. The durable design-directions artifact is authored only after grilling, so it cannot supply the missing visual comparison at decision time. Fix: align spec.md, decisions.md, tasks.md Step 2, and AC1–AC2 on a supported route—either use standard AskUserQuestion labels/descriptions and revise the rendered-preview requirement, or reorder the workflow so a project-local artifact can be authored and published before the taste decision.
- **Lead-owned file writes still contradict the build lead's no-edit contract.** `.claude/commands/harness-layer/harness-build.md` currently says the lead “never edit[s] files directly”; tasks.md Step 3 scopes the new exception only to folding `implementation-notes.md`, yet also requires creating that file and says the lead authors `artifacts/ship-brief.html` in every approving round. A builder cannot implement all three instructions without inventing another exception or owner. Fix: update decisions.md, tasks.md Step 3, AC3, and AC5 to explicitly include notes creation/folding and ship-brief authoring in a narrowly defined lead-owned non-implementation-file exception, or assign those writes to named agents while preserving the approval-commit ordering.

**Recommendations (advisory, non-blocking):**

- Define the report-plus-brief operation once as an approval-finalization procedure referenced by the clean round-1, approved round-2, and approved round-3 branches; this is shorter and reduces sequencing drift between approval paths.

**Issue-comment digest:** Round 2, changes-requested — 2 blocking: the taste route relies on an unsupported AskUserQuestion preview contract, and required lead-owned notes/brief writes conflict with the build lead's no-direct-edit rule. Next: revise those contracts, then re-review.

### Round 1 — Verdict: changes-requested

- **The trimmed `worktree/_common.py` contract omits a runtime dependency of one of its five required helpers.** `read_payload()` reads `STDIN_TIMEOUT`, but spec.md and tasks.md require copying exactly the five functions plus imports, and AC2 requires exactly those helpers; a literal implementation therefore raises `NameError` on every payload read. Fix: amend spec.md's requirement, tasks.md Step 1, and AC2 to include the unchanged `STDIN_TIMEOUT = 5.0` module constant (and validate the allowed symbols), or explicitly rewrite `read_payload()` to remove that dependency while preserving behavior.
- **Per-fixer effort selection has no executable deployment path in the plan.** tasks.md Step 2 and AC4 require the build lead to choose model and effort per fixer, but `.claude/rules/task-tools.md` documents no `effort` argument on `Agent(...)`; the cached subagent docs expose effort through agent definitions/frontmatter, while agent-team teammates inherit the lead's effort (`ai-docs/anthropic/subagents.md`, lines 224 and 270–284; `ai-docs/anthropic/agent-teams.md`, lines 130–141). Fix: specify and scope a supported mechanism—such as named fixer definitions carrying the selected effort, or an explicit task-tools contract update—and add a validation that the generated invocation can actually express both values; otherwise narrow the requirement to supported model-only or inherited-effort behavior.
- **The validation commands do not prove several load-bearing acceptance claims.** The AC4 checks only reject one `sonnet` phrase and find the manifest/assignee strings; they do not verify positive model+effort routing, parallel disjoint fix clusters, one combined fix commit, concurrent unblocked implementation, or type+priority label mirroring. AC2 likewise does not verify the exact helper surface or unchanged moved scripts, and AC3 does not verify the five command identities or the stated single PostToolUse `Bash` block. Fix: extend acceptance-criteria.md with deterministic AST/text checks for AC2/AC4 and `jq` assertions for every AC3 command/block, with each command mapped to the precise clause it proves.

**Recommendations (advisory, non-blocking):**

- `ai-docs/astral/uv-scripts.md` documents `uv run --script` execution but not the claimed insertion of the script directory into `sys.path`; add an official Python import-path source through `/kb add`, or phrase this as a runtime invariant proved by the hermetic tests rather than as uv-doc-grounded behavior.
- Reconcile the cached hooks sources before future plans rely on `ai-docs/anthropic/hooks.md`: it describes `.claude/hooks.json` and WorktreeCreate as an async, non-returning event, while the newer cached settings/guide material and `ai-docs/anthropic/worktrees.md` use settings hooks and a stdout worktree path. A `/kb` refresh should leave one authoritative contract.

**Issue-comment digest:** Round 1, changes-requested — 3 blocking: the trimmed helper omits `STDIN_TIMEOUT`, per-fixer effort has no supported deployment path, and validation does not prove core AC2–AC4 claims. Next: repair those three contracts, strengthen the validation commands, then re-review.

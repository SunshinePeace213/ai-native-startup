### Round 2 — Verdict: changes-requested

- **AC2's substitution/provenance requirement is not testable.** AC2 requires every identity-table substitution to be recorded in `decisions.md` and prohibits hand-authored mirrors, but its validation commands only check manifest fields and mirror frontmatter; a substituted URL can lack a recorded addendum and a hand-authored file can satisfy those checks. Fix: add an observable build record for each `/harness-layer:kb add` result (or make the command output available to validation), then add a validation command that compares each final URL with the identity table, requires a `decisions.md` addendum for every deviation, and verifies every mirror came from a successful kb-fetcher result.
- **AC6's exact-surface command is sequenced before the commit it inspects.** Step 4 runs validation immediately after the builder edits files, but `git diff --name-only origin/main...HEAD` ignores those uncommitted edits and therefore fails its new exact-path assertion. Fix: in `acceptance-criteria.md`, compare `origin/main` directly with the working tree (including staged and unstaged edits), or add an explicit commit prerequisite before Step 4 and retain the `HEAD` comparison.

**Issue-comment digest:** Round 2, changes-requested — 2 blocking: AC2 cannot verify required source substitutions or mirror provenance, and AC6 checks HEAD before the plan commits the built changes. Next: make both validations observable and correctly sequenced, then re-review.

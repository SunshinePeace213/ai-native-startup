### Round 3 — Verdict: changes-requested

- **The documented fetch-failure path cannot meet AC2's required manifest shape.** `/harness-layer:kb add` appends the original entry and, on `FAIL`, leaves it with `fetched: null`; adding a substitute therefore leaves a sixth `design` entry (or requires an unplanned manual manifest edit), while AC2 requires exactly five fully fetched entries and the plan promises six `add` runs. Fix: in decisions.md and tasks.md, define the supported replacement operation for the failed provisional entry and its order, then update AC2 and its provenance assertion so the final manifest, number of runs, and run record are achievable after a substitution.
- **AC2 contradicts the plan's general substitution rule for web.dev and Google Fonts.** spec.md and decisions.md allow a canonical same-topic substitute when a source refuses fetching, but the AC2 validation command unconditionally requires URLs containing `web.dev/learn/design` and `fonts.google.com/knowledge`; a documented swap for either still fails before the swap-record check. Fix: either lock substitutions to the specified W3C fallback and make failures of the other fixed sources a stop condition, or list and validate permitted canonical replacements for each fixed source.

**Issue-comment digest:** Round 3, changes-requested — 2 blocking: the substitution flow leaves an unresolvable failed manifest entry/run-count conflict, and AC2 rejects allowed web.dev or Google Fonts substitutions. Next: make the failure/replacement contract and its assertions consistent, then re-review.

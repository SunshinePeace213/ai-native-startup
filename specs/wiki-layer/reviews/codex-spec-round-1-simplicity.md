- [STD:S3] [S:major] [C:99] `spec.md` Wiki seed shapes / `tasks.md` §3 — incompatible privacy and bookkeeping contracts at the personal-domain boundary — evidence: personal content is “gitignored and local-only,” but every ingest “updates index + log,” both of which are tracked shared files containing page and operation titles — fix: keep personal entries out of the shared index/log and maintain ignored `personal/index.md` and `personal/log.md` files.
- [STD:S3] [S:major] [C:96] `spec.md` Wiki seed shapes / `tasks.md` §4 — read-only operations are included in a mutation contract they cannot implement — evidence: `log.md` permits `query|status` entries while query is “strictly read-only on the wiki,” and neither query nor status is instructed to append a log entry — fix: restrict the log to mutating operations (`ingest|lint`) or explicitly make and specify logging as the sole permitted write.
- [STD:S3] [S:major] [C:97] `tasks.md` §4 Commands: query + status — status requires state that the design never creates — evidence: status must report the “unprocessed-source backlog vs the absorb threshold,” but there is no source queue, processed-source manifest, or stable source identifier in the log, and `absorb` is deferred — fix: remove the backlog readout from v1 or define a single authoritative source inventory and processed-state mapping first.
- [STD:S3] [S:major] [C:94] `spec.md` Edge Cases / Wiki seed shapes — duplicate-ingest idempotency lacks a usable identity mechanism — evidence: duplicate detection says “log is checked,” while the only log key is `## [YYYY-MM-DD] <op> | <title>`, which cannot distinguish different sources with the same title or renamed sources — fix: check canonical source paths in page `sources` frontmatter, or add a stable source path/hash to ingest log records.
- [STD:S3] [S:major] [C:99] `tasks.md` §7 / `acceptance-criteria.md` AC7 — final validation is ordered before an acceptance criterion it claims to satisfy and cannot perform its required write — evidence: `validate-all` has “Files: none — read-only” and treats AC7 as merely documented, while AC7 is “manual, user-run, post-ship” and requires output recorded in `implementation-notes.md` — fix: run the pilot before final validation with `implementation-notes.md` in scope, or explicitly remove AC7 from pre-ship done and from `validate-all`’s claimed coverage.
- [STD:S3] [S:minor] [C:90] `acceptance-criteria.md` AC7 — the empty-wiki pilot requires a relationship that its fixture does not guarantee — evidence: the first fresh article must produce “≥1 `[[wikilink]]`,” while the seed contains no substantive pages and the writing rules prohibit thin or unnecessary pages — fix: require a wikilink only when a legitimate related page exists, or seed the pilot with two related sources.
- [STD:S3] [S:minor] [C:99] `tasks.md` §1 / `spec.md` Obsidian vault config — a required file has no executable content contract — evidence: the task says to write `appearance.json` “(defaults) per the contract block,” but that block defines only `app.json` — fix: remove `appearance.json` from the plan or specify its exact minimal JSON.
- [STD:S8] [S:major] [C:97] `tasks.md` §§3, 4, 6 / `checks/ac3-commands.py` / `checks/ac4-standards-rule.py` — the same structural contract is implemented in two permanent verification layers — evidence: AC3/AC4 scripts check command frontmatter and rule scoping, then `test_wiki_layer.py` is tasked to repeat those exact assertions — fix: put the durable assertions only in `test_wiki_layer.py` and have plan validation invoke that test instead of maintaining duplicate scripts.
- [STD:S8] [S:minor] [C:98] `tasks.md` §3 Commands: ingest + lint — batch ingestion and checkpoint machinery exceed the v1 objective and pilot — evidence: ingest accepts a “file/folder” and adds “batch input checkpoints every 5 sources,” while the pilot and cited preferred workflow ingest one source at a time — fix: ship single-source ingest in v1 and add folder batching only after observed demand.
- [STD:S8] [S:minor] [C:91] `spec.md` Wiki page frontmatter — `related` duplicates the graph already represented by body wikilinks — evidence: every page carries `related: ["[[Other Page]]"]` while the linking contract separately requires `[[wikilinks]]` between pages — fix: derive relationships from body wikilinks and remove the mandatory `related` field.
- [STD:S8] [S:minor] [C:96] `spec.md` AGENTS.md amendments — the new always-loaded section repeats information already established by the rewritten Knowledge Base bullets — evidence: both blocks describe `ai-docs/wiki/`, its relationship to mirrors, and its catalog, while the new section adds a separate heading primarily to list commands — fix: fold the command and standards-rule pointers into one Knowledge Base bullet and omit the new section.
- [STD:S8] [S:minor] [C:99] `tasks.md` §2 / `acceptance-criteria.md` AC4 — optional plugins unrelated to v1 behavior are made normative and test-required — evidence: Dataview and Marp must appear in the standards rule although neither is required by any operation or pilot; only Web Clipper participates in AC7 — fix: retain Web Clipper guidance and remove Dataview/Marp from the v1 contract and check.
- [STD:S8] [S:minor] [C:93] `tasks.md` §4 Commands: query + status — fixed retrieval numbers add policy without improving the objective — evidence: query must read “3–8 pages” and follow links “up to 2 hops,” even when a complete answer needs fewer or more — fix: instruct query to read the smallest relevant page set and follow links only while they materially contribute evidence.
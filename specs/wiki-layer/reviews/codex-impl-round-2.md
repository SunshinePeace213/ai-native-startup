- [STD:I1] [S:major] [C:98] .claude/commands/wiki/ingest.md changed-source re-ingest — every non-identical ingest must update both the domain index and log while preserving canonical-key uniqueness — evidence: lines 67–69 say “refresh the existing INDEX rows only; the LOG keeps its one entry,” conflicting with wiki-standards.md:127: “100% of ingests update the index and the log” — fix: define a non-duplicating log update for changed-source re-ingests and apply it in both Instructions and Workflow.
- [STD:I1] [S:major] [C:100] specs/wiki-layer/implementation-notes.md AC7 evidence — the required three-run eval must retain per-run query citations and lint outputs; structural residue cannot prove those behavioral rubric conditions — evidence: lines 258–263 explicitly state those outputs “were not captured at the time, and therefore gone” while AC7 requires per-run evidence — fix: rerun the fixture flow three times in fresh sessions and record every rubric output, or mark AC7 incomplete.
- [STD:I8] [S:major] [C:98] specs/wiki-layer/checks/ac2-seed.py MD024 regression check — a regression test must reproduce the lint behavior, not merely detect an implementation string anywhere in the file — evidence: lines 82–83 only test `if "<!-- markdownlint-disable MD024 -->" not in text`, which passes if the directive is inside a code fence or otherwise ineffective — fix: exercise markdownlint against a temporary log containing duplicate sibling headings, or structurally require an effective file-level directive outside fenced content.
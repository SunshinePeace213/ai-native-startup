- [STD:I6] [S:major] [C:100] `.claude/commands/harness-layer/harness-build.md:36` — drive-by scope expansion beyond the wiki-layer task boundary — evidence: the diff adds mirror-copy behavior despite the guardrail “do not touch … any `harness-layer` command file” — fix: revert this change and deliver it through a separately scoped plan.
- [STD:I5] [S:major] [C:96] `.claude/commands/wiki/*` command resolution — namespaced commands must trace to a cited official command-layout contract — evidence: `skills.md:309` documents namespacing for nested `.claude/skills/`, while line 310 only maps a file directly under `.claude/commands/` to its filename; nothing cited establishes `.claude/commands/wiki/ingest.md` → `/wiki:ingest` — fix: add an official mirrored reference explicitly documenting nested command-directory resolution, or use a documented layout and update the locked interface.
- [STD:I5] [S:minor] [C:98] wiki command `model` frontmatter — every selected model alias must be grounded by a listed KB reference — evidence: `skills.md:269` delegates valid values to `/model` but none of the nine references enumerates `opus`, `sonnet`, or `haiku`; `model-config.md` is absent from `## KB References` — fix: add and verify the official model-config mirror in the reference table.
- [STD:I1] [S:major] [C:94] `.claude/commands/wiki/ingest.md:22-36,51-53` — idempotency must use stores that actually contain the canonical key and distinguish changed from identical input — evidence: KEY is recorded only in page frontmatter and LOG, yet the workflow requires finding it in INDEX, then says presence both triggers an update and allows an identical no-op — fix: match KEY only in page `sources:` and LOG, compare source-derived content, no-op identical input, and update pages/index only when content changed.
- [STD:I1] [S:major] [C:100] `.claude/commands/wiki/lint.md:33-34,46-50` — contradiction handling must have one executable outcome — evidence: Checks says “Flag both sides `disputed`,” while Fix or report says contradictions are reported “without rewriting the pages” — fix: require lint to mark both pages disputed and cross-link them, while leaving only resolution/supersession for judgment.
- [STD:I1] [S:major] [C:97] `.claude/commands/wiki/lint.md:18-20` — every lint pass must append its required log state, including an empty-wiki pass — evidence: “report clean and stop” exits before `## Log the pass`, although the task requires lint to append an entry — fix: skip substantive checks on the seed but continue through logging and reporting.
- [STD:I1] [S:major] [C:100] `.claude/commands/wiki/status.md:25-31` — expansion triggers must remain derived from tracked state in edge cases — evidence: backlog `>10` must fire absorb, but the seed-only branch says “no triggers fired”; the manifest contains 53 entries, so a seed wiki should fire absorb — fix: compute absorb independently on every run and apply seed/no-history exceptions only to breakdown and cleanup.
- [STD:I1] [S:major] [C:96] `.claude/commands/wiki/lint.md:24; .claude/commands/wiki/status.md:22` — orphan detection must consistently identify pages lacking inbound wiki links — evidence: lint additionally requires no index row, making every correctly indexed orphan invisible, while status additionally requires no outgoing links — fix: define and use one no-inbound-link rule; keep missing index rows as the separate index-drift class.
- [STD:I1] [S:minor] [C:100] `ai-docs/wiki/index.md:16,22,28,34` — empty domain tables must contain no data rows — evidence: Business, Development, Books, and Articles each contain a second `| --- | --- | --- | --- |` row after the delimiter — fix: remove the four duplicate rows and make the seed validator reject non-delimiter rows in domains not populated by the pilot.
- [STD:I1] [S:major] [C:100] `specs/wiki-layer/implementation-notes.md:117-177` — AC7 requires exact, per-run evidence for every rubric condition — evidence: runs 2–3 only say the query had citations and lint was clean without recording the query answer/citation targets or lint output, and run 2 reports “page count 4 → 4” despite the two-page pilot — fix: record the exact command sequence, page paths, index/log entries, query answer with citation targets, and lint report for each run, correcting the page-count discrepancy.
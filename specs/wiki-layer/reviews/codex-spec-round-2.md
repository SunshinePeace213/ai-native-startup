- [STD:S2] [S:major] [C:99] checks/ac2-seed.py — validators must reject every malformed form within an acceptance-criterion shape — evidence: lines 46–49 only forbid a table in Personal, permitting arbitrary extra content despite AC2 requiring “only the local-only pointer”; table headers and operation vocabulary are likewise partial regex matches — fix: parse exact section bodies, complete Markdown tables, and the complete allowed operation set.
- [STD:S2] [S:major] [C:99] tasks.md §6 — interface-contract tests must assert exact deterministic frontmatter at the command boundary — evidence: lines 171–174 check only non-empty description, roster membership, and argument-hint presence, while spec.md calls these “exact keys” and assigns command-specific model/effort values — fix: assert each command’s exact key set and command-specific deterministic values from a declared source of truth.
- [STD:S2] [S:major] [C:99] tasks.md §6 — structural validators must cover every normative property claimed by their acceptance criterion — evidence: lines 175–180 assert headings, schema fields/statuses, and shared-index exclusion but omit AC4’s citation rule, writing rules, six-domain contents, personal assets, secret/PII stripping, plugin set, metrics, lane fit, and archetypes — fix: add section-scoped assertions for every AC4 obligation.
- [STD:S2] [S:major] [C:100] checks/ac5-memory-amendments.py — budget validators must measure the full boundary named by the acceptance criterion — evidence: `WIKI_SECTION_BUDGET = 8` and lines 61–63 count only the Wiki Layer section, while AC5 limits the total AGENTS.md addition to 14 lines — fix: compare the complete amendment against the baseline or exact prescribed rewrite and fail when total added lines exceed 14.
- [STD:S3] [S:major] [C:98] tasks.md §3–4 — derived status outputs require an earlier producer and stable persisted schema for every input — evidence: lines 133–137 consume repeated missing-page flags and mechanical-fix counts from lint log entries, but lines 103–111 require only that lint “appends a log entry,” and the log contract defines no payload for either value — fix: define the lint-entry payload and require lint to record both fields before status consumes them.
- [STD:S2] [S:critical] [C:100] tasks.md §7 / decisions.md Assumptions — nondeterministic command behavior must use the repository’s eval tier across repeated runs — evidence: tasks.md line 188 calls one real-session run an “eval tier,” while decisions.md lines 149–150 explicitly replace the eval harness with a manual pilot; test-tiers.md line 24 states “a single green run proves nothing” — fix: run the fixtures through the meta-skills eval harness for N repeated runs and record the rubric pass rate.
- [STD:S5] [S:minor] [C:100] spec.md Objective — completion claims must match the validators retained by the plan — evidence: line 33 still requires “five plan-local checks,” but this diff deletes ac3 and ac4, leaving only ac1, ac2, and ac5 as plan-local executable checks — fix: update the objective to the actual three checks plus drift suite and eval, or restore five executable checks.
- [STD:S5] [S:minor] [C:100] spec.md Relevant Files / Notes — KB registration state must be described consistently across the current specification — evidence: lines 212–213 call sources.yaml “untouched” and lines 286–288 still describe routines as a KB gap, while this diff registers routines in sources.yaml and decisions.md says it was mirrored and registered — fix: mark the manifest addition explicitly and remove the completed gap note.
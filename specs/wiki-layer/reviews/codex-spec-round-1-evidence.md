- [STD:S2] [S:major] [C:99] specs/wiki-layer/checks/ac1-privacy-gitignore.sh — privacy-boundary validation samples ignore patterns but does not prove the protected namespace contains no tracked files or cover every shared domain — evidence: `git check-ignore` is run only on hypothetical personal paths and `engineering/example-topic.md`; no `git ls-files ai-docs/wiki/personal/` assertion exists — fix: assert zero tracked personal files, test every shared-domain namespace and representative `workspace*` variants, and verify the required gitignore rule ordering.
- [STD:S2] [S:major] [C:100] specs/wiki-layer/checks/ac2-seed.py — compound interface shapes are reduced to unscoped substrings and one partial JSON property, allowing malformed seeds to pass — evidence: `if domain not in text.lower()`, `if "## [" not in text`, and only `attachmentFolderPath` is checked — fix: parse exact domain headings and per-section tables, columns, local-only marker, purpose/footer, log heading/format/op vocabulary/empty seed, and both `app.json` properties including `alwaysUpdateLinks`.
- [STD:S2] [S:major] [C:99] specs/wiki-layer/checks/ac3-commands.py — the exact command-frontmatter contracts are not asserted structurally — evidence: the hand-rolled parser only requires nonempty generic fields and membership in hard-coded sets, so incorrect per-command models, efforts, argument hints, descriptions, invalid YAML, or extra operations can pass — fix: parse YAML with the project parser, compare each command against its exact contract, reject unplanned command files, and derive allowed model/effort pairs from `model-selection.md`.
- [STD:S2] [S:critical] [C:100] specs/wiki-layer/tasks.md §3–4 / checks/ac3-commands.py — behavior-changing command tasks have no eval-tier proof and do not name eval files in **Files**, so all four “working” operations can pass validation with nonfunctional bodies — evidence: task Files list only command Markdown, while the validator inspects frontmatter and merely searches query for `"read-only"`; test-tiers.md assigns commands to repeated-run evals — fix: add named eval cases, rubric, runner, and fixtures to both tasks’ Files and validation commands, covering operation workflows and specified edge cases over repeated runs.
- [STD:S2] [S:major] [C:100] specs/wiki-layer/checks/ac4-standards-rule.py — whole-file keyword presence does not prove the standards rule states the required structured schema or normative policies — evidence: fields pass when `field in lower`, scoping passes on `"paths:"` and `"ai-docs/wiki/**"` substrings, and topics such as lane fit, metrics, and archetypes require only the words `"lane"`, `"metric"`, and `"archetype"` — fix: parse YAML scoping and structured sections, assert exact fields/vocabularies, status propagation, privacy/PII, writing/linking rules, plugin set, lane rules, metric targets, and archetype assignments in a source-derived drift test named in the rule task’s Files.
- [STD:S2] [S:major] [C:100] specs/wiki-layer/checks/ac5-memory-amendments.py — global marker searches neither localize the required amendments to their sections nor enforce the ≤14-line budget — evidence: the script uses first-occurrence `text.index`, accepts `"crystalliz"` anywhere, accepts any single `"/wiki:"`, and contains no line-count assertion — fix: parse the Knowledge Base, durability-rule, and Wiki Layer sections; assert their ordered exact obligations and all four command pointers; then calculate and cap the amendment’s added lines against the plan baseline.
- [STD:S2] [S:major] [C:96] specs/wiki-layer/tasks.md §6 / acceptance-criteria.md AC6 — the planned drift test derives the expected command set from the directory being tested, creating a vacuous boundary that cannot detect missing commands — evidence: “re-derive the expected command set from `.claude/commands/wiki/*.md`” — fix: derive the required set from an independent authoritative registration or manifest, compare it exactly with the filesystem, require a nonempty set, and parse the model/effort roster from its source.
- [STD:S2] [S:major] [C:100] specs/wiki-layer/acceptance-criteria.md AC7 / tasks.md §7 — the end-to-end criterion has no checked-in command that fails on reversion and is treated as complete before execution — evidence: validation is `manual: user clips...`, while `validate-all` verifies only that the “pilot procedure [is] documented and runnable” and execution occurs post-ship — fix: add a checked-in fixture-driven pilot/eval command that validates produced page schema, links, citations, index/log registration, query citation/status behavior, and lint output, or move the manual pilot outside acceptance and replace AC7 with an executable pre-ship criterion.
- [STD:I2] [S:major] [C:99] specs/wiki-layer/acceptance-criteria.md:94-100 — validation commands must name runnable pytest nodes or plan-check scripts, not narrative procedures — evidence: `manual: build lead runs the fixture flow three times...` — fix: add a runnable `specs/wiki-layer/checks/` verifier and reference its path.
- [STD:I2] [S:major] [C:98] tests/harness-layer/test_wiki_layer.py:140-186 — AC3 evidence must prove parseable, non-empty frontmatter and a read-only contract, not merely key presence and a token match — evidence: no assertion checks non-empty `description`/`argument-hint`, and query passes on `re.search(r"read-only", ...)` even if negated — fix: parse YAML, assert required values are non-empty strings, and test the explicit prohibition on writes.
- [STD:I2] [S:major] [C:95] tests/harness-layer/test_wiki_layer.py:189-270 — AC4 evidence must prove each normative obligation rather than isolated keywords — evidence: privacy passes whenever `"secret"` and `"pii"` occur, plugin checks require names only, and writing checks omit flat tone, quote discipline, and length bounds — fix: assert section-scoped normative statements for every AC4 obligation, including directionality and the omitted writing requirements.
- [STD:I2] [S:major] [C:100] specs/wiki-layer/checks/ac5-memory-amendments.py:94-98 — AC5’s passing check must measure the actual ≤14-line amendment boundary — evidence: `added = len(WIKI_SECTION_LINES)` measures the test fixture itself, not the AGENTS.md diff — fix: compare AGENTS.md against the specified base revision and count actual added lines within the amendment.
- [STD:I2] [S:major] [C:100] specs/wiki-layer/implementation-notes.md:117-177 — manual-check evidence must record each run’s exact commands and observed outputs — evidence: runs 2–3 say `"same four-command flow"` and only claim that query synthesized and lint was clean; no query answers, citation output, lint output, or exact commands are recorded — fix: record a complete per-run transcript with exact command arguments and outputs for all four operations.
- [STD:I3] [S:major] [C:100] specs/wiki-layer/implementation-notes.md hand-offs:42-98,178-185 — every hand-off must preserve exact commands and observed results — evidence: commands are abbreviated as `python -c "…split('---')[1]…"`, markdownlint is described only as “on the file,” full reads have no recorded probe, and `validate-all` reports only `AC1 → ... AC7` labels — fix: replace summaries and ellipses with every literal command executed and its exact observed exit status/output.
- [STD:I8] [S:major] [C:100] specs/wiki-layer/checks/ac5-memory-amendments.py:94-98 — a check must derive its result from the subject under test rather than hard-coded expected data — evidence: `added = len(WIKI_SECTION_LINES)` can never detect unrelated added AGENTS.md lines — fix: compute the actual diff addition count and add a negative test demonstrating that a 15-line amendment fails.
- [STD:I8] [S:major] [C:99] commit 992587f / specs/wiki-layer/implementation-notes.md:148-165 — a bug fix must carry a failing regression test that reproduces the defect — evidence: the MD024 repeated-heading fix changes only `lint.md`, `log.md`, and notes; verification is an uncommitted “scratch-copy proof” — fix: add a committed regression check containing duplicate same-day lint headings, demonstrate failure before the fix, and include it in Validation Commands.
---
paths:
  - "products/**/*"
  - ".claude/commands/c-suite/**"
---

# CPO Department Lessons

Append one line per lesson learned during an engagement, oldest first:
`- <date> | <stage> | <pitfall> → <prevention rule>`

- 2026-07-17 | intake | persona agent types created mid-session don't resolve in the same session's registry → deploy personas via general-purpose + brief carrying the persona file and SKILL.md paths when the custom type is unavailable
- 2026-07-17 | brief | a route-back fix pass introduced a new blocking defect (a form label contradicting the package's own contact rule) → after any fix pass, re-verify the exact contract lines the fix touched before re-review, and budget Design-Lead re-reviews per finding generation
